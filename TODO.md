# TODO

1. Genbesøg `printf` afsnittet og erstat `System.out.printf` med `String.format` og `IO.println`. 
2. Genbesøg `Scanner` afsnittet og erstat `Scanner` med `IO.read*` metoderne med mindre `Scanner` bruges som tekstscanner, altså ikke til at læse fra konsollen, hvor det er bedre at bruge `IO.read*` metoderne.